apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ha-claim
  namespace: ha
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
